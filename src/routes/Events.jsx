import NavBar from '../components/navbar';
import '../stylesheets/events.css'
function Gallery() {
    return(
        <div>
            <NavBar page='Events' />
            {/*Paste the events code after this line, no need to provide navbar because it is added before this line and remove this line before contributing*/}
        </div>
    );
}

export default Gallery;